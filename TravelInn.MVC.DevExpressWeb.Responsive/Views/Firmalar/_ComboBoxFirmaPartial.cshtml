@Html.DevExpress().ComboBox(settings => {
    settings.Name = "comboBoxFirmaSearch";
    //settings.Width = 180;
    //settings.SelectedIndex = 0;
    settings.CallbackRouteValues = new { Controller = "Firmalar", Action = "ComboBoxFirmaPartial" };
    settings.Properties.CallbackPageSize = 1000;
    settings.Properties.TextField = "Ismi";
    settings.Properties.ValueField = "Id";
    settings.Properties.ClearButton.DisplayMode = ClearButtonDisplayMode.OnHover;
    settings.Properties.DropDownHeight = Unit.Pixel(450);

    settings.Properties.ClientSideEvents.ValueChanged = "function(s,e){ if(s.GetValue()){location.href = '/CariVeOdemeler/?frmId=' + s.GetValue()};}";
    settings.Properties.ClientSideEvents.Init = "function(s,e){ comboFirmaSearchSelectFromUrl(s,e) }";

}).BindList(Model).GetHtml()
